fn main() -> int:
    let temp = 0
    if temp == 0:
        let interna = 42
        temp = interna
    # Se tentarmos usar 'interna' aqui, o compilador deve dar erro!
    # interna = 10
    return temp
